# Production Environment Variables for Archevi Frontend
#
# Copy this to .env.production and fill in your values
# These are baked into the build at compile time

# Windmill API URL - REQUIRED for production
# Must be HTTPS and match your CORS configuration
VITE_WINDMILL_URL=https://api.archevi.ca

# Windmill workspace name
VITE_WINDMILL_WORKSPACE=archevi

# Windmill token - for production, consider using user auth instead of static token
# If using static token, ensure it has minimal required permissions
# VITE_WINDMILL_TOKEN=your-production-token-here

# Note: For production, the app should authenticate users via the auth_login
# endpoint and use their JWT tokens, not a static API token.
